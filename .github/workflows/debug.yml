name: Debug
on: push
jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - uses: appleboy/ssh-action@master
        with:
          host: "185.255.93.94"
          username: "root"
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 2222
          script: |
            docker logs serenut-backend
