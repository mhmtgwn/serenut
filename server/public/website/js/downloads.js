const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
document.addEventListener('DOMContentLoaded',async()=>{const root=document.getElementById('downloads-container');if(!root)return;root.innerHTML='<div class="state-card">Sürümler yükleniyor…</div>';try{const res=await fetch('/api/v1/releases/history');if(!res.ok)throw new Error('Sürüm servisi yanıt vermedi.');const all=await res.json();const seen=new Set();const rows=all.filter(r=>{const p=String(r.platform).toLowerCase();if(seen.has(p))return false;seen.add(p);return true});root.innerHTML=rows.map(r=>`<article class="feature-card"><div class="eyebrow">${esc(String(r.platform).toLowerCase()==='android'?'Android APK':'Windows')}</div><h3>Serenut OS ${esc(r.version_code)}</h3><p>${esc(r.release_notes||'Kararlılık ve performans güncellemeleri.')}</p>${r.is_available?`<a class="btn btn-primary" href="/api/v1/updates/download/${encodeURIComponent(String(r.platform).toLowerCase())}/latest">Paketi İndir</a>`:'<button class="btn btn-secondary" disabled>Yakında</button>'}</article>`).join('')||'<div class="state-card">Yayınlanmış sürüm bulunamadı.</div>';}catch(e){root.innerHTML=`<div class="state-card state-error">${esc(e.message)}</div>`}});
